Name: Workflow_d94d
SchemaVersion: "1.0"

# Optional - Set automatic triggers.
Triggers:
  - Type: Push
    Branches:
      - main

# Required - Define action configurations.
Actions:
  DeployWithAWSSAM_f5:
    # Identifies the action. Do not modify this value.
    Identifier: codecatalyst-labs/deploy-with-sam@v1.0.1

    # Specifies the source and/or artifacts to pass to the action as input.
    Inputs:
      # Required
      Sources:
        - WorkflowSource # This specifies that the action requires this Workflow as a source

    # Required; You can use an environment, AWS account connection, and role to access AWS resources.
    Environment:
      Name: sazecis
      Connections:
        - Name: 328764087071
          Role: CodeCatalystWorkflowDevelopmentRole-sazecis

            # Defines the action's properties.
    Configuration:
      # Required; type: string; description: The name of the AWS CloudFormation stack that you're deploying to. If you specify an existing stack, then the command updates the stack. If you specify a new stack, then the command creates it.
      StackName:
        sybiotecatalyst


